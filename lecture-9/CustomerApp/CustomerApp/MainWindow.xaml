<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CustomerApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kundhantering"
    Closed="Window_Closed">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <NavigationView x:Name="AppNavigation"              
                    ItemInvoked="AppNavigation_ItemInvoked"
                    IsSettingsVisible="True"
                    IsBackEnabled="False"
                    IsBackButtonVisible="Collapsed"
                    >
        
        <NavigationView.MenuItems>
            <NavigationViewItem x:Name="CustomersNavigationItem" Content="Kunder" Tag="customers" Icon="People" />
        </NavigationView.MenuItems>

        <Border>
            <ContentControl Content="{x:Bind ViewModel.Navigation.CurrentPage, Mode=OneWay}" HorizontalContentAlignment="Stretch" VerticalAlignment="Stretch" />
        </Border>
        
    </NavigationView>
</Window>
