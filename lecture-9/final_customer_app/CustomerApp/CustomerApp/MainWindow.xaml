<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CustomerApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kundhantering">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <NavigationView
        x:Name="AppNavigation"
        PaneTitle="Kundhantering"
        Style="{StaticResource AppNavigationStyle}"
        ItemInvoked="AppNavigation_ItemInvoked">

        <NavigationView.MenuItems>
            <NavigationViewItem
                x:Name="CustomersNavigationItem"
                Content="Kunder"
                Tag="customers"
                Icon="People" />
        </NavigationView.MenuItems>

        <Border Style="{StaticResource PageSurfaceStyle}">
            <ContentControl
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{x:Bind ViewModel.Navigation.CurrentPage, Mode=OneWay}" />
        </Border>
    </NavigationView>
</Window>